node_modules/
out/
dist/
*.log
.DS_Store
.vscode/
.idea/

*storybook.log
storybook-static
# storybook-design-token build artifact (regenerated on each storybook build)
public/design-tokens.source.json

*.local.*

# Claude Code session runtime artifacts (e.g. scheduled-tasks locks)
.claude/*.lock
# Claude Code scratch artifacts (token allowlist, transient logs)
.claude/artifacts/
